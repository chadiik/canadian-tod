# line preview style "spray" or "stroke"
linestyle=stroke

# if debug is true, will use sample video file; Otherwise stream from webcam
debug=false

# use serial streaming or mock it
stream=true

# sample is the video entry for debug mode
sample=faces.mp4

# sketch routine's version: 'legacy' (orbital path tracing) or 'hatch'
version=legacy

# starting cell (0-5)
cell=0

wallConfig=wall

xOffset=2300
yOffset=500

xScale=4
yScale=.92

skewZ=1550
skewDelta=740

idleX=100
idleY=1200